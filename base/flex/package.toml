[package]
name = "flex"
version = "2.6.4"
description = "Fast lexical analyzer generator (Arch version)"

# Phase 1 schema migration — defaults to layout = "system"; revisit per-port later.
[install]
layout = "system"

[build]
source = "https://github.com/westes/flex/releases/download/v2.6.4/flex-2.6.4.tar.gz"
checksum = ""
type = "autotools"
