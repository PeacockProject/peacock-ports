[package]
name = "bison"
version = "3.8.2"
description = "GNU parser generator (Arch version)"

# Phase 1 schema migration — defaults to layout = "system"; revisit per-port later.
[install]
layout = "system"

[build]
source = "https://ftp.gnu.org/gnu/bison/bison-3.8.2.tar.xz"
checksum = ""
type = "autotools"
