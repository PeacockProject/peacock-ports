diff --git a/src/fbdev.c b/src/fbdev.c
index f6ad6a3..8f61147 100644
--- a/src/fbdev.c
+++ b/src/fbdev.c
@@ -596,6 +596,7 @@ FBDevPreInit(ScrnInfoPtr pScrn, int flags)
 	switch ((type = fbdevHWGetType(pScrn)))
 	{
 	case FBDEVHW_PACKED_PIXELS:
+	packed_preinit:
 		switch (pScrn->bitsPerPixel)
 		{
 		case 8:
@@ -628,10 +629,11 @@ FBDevPreInit(ScrnInfoPtr pScrn, int flags)
                           "EGA/VGA planes are not yet supported by the fbdev "
 			  "driver\n");
                return FALSE;
-       default:
-               xf86DrvMsg(pScrn->scrnIndex, X_ERROR,
-                          "unrecognised fbdev hardware type (%d)\n", type);
-               return FALSE;
+	   default:
+	               xf86DrvMsg(pScrn->scrnIndex, X_WARNING,
+	                          "unrecognised fbdev hardware type (%d), treating as packed pixels\n",
+	                          type);
+	               goto packed_preinit;
 	}
 	if (xf86LoadSubModule(pScrn, "fb") == NULL) {
 		FBDevFreeRec(pScrn);
@@ -840,6 +842,7 @@ FBDevScreenInit(SCREEN_INIT_ARGS_DECL)
 	switch ((type = fbdevHWGetType(pScrn)))
 	{
 	case FBDEVHW_PACKED_PIXELS:
+	packed_screeninit:
 		switch (pScrn->bitsPerPixel) {
 		case 8:
 		case 16:
@@ -885,11 +888,9 @@ FBDevScreenInit(SCREEN_INIT_ARGS_DECL)
 		ret = FALSE;
 		break;
 	default:
-		xf86DrvMsg(pScrn->scrnIndex, X_ERROR,
-		           "internal error: unrecognised hardware type (%d) "
-			   "encountered in FBDevScreenInit()\n", type);
-		ret = FALSE;
-		break;
+		xf86DrvMsg(pScrn->scrnIndex, X_WARNING,
+		           "unrecognised fbdev hardware type (%d), treating as packed pixels\n", type);
+		goto packed_screeninit;
 	}
 	if (!ret)
 		return FALSE;
@@ -945,6 +946,7 @@ FBDevScreenInit(SCREEN_INIT_ARGS_DECL)
 	{
 	/* XXX It would be simpler to use miCreateDefColormap() in all cases. */
 	case FBDEVHW_PACKED_PIXELS:
+	packed_cmap:
 		if (!miCreateDefColormap(pScreen)) {
 			xf86DrvMsg(pScrn->scrnIndex, X_ERROR,
                                    "internal error: miCreateDefColormap failed "
@@ -968,10 +970,9 @@ FBDevScreenInit(SCREEN_INIT_ARGS_DECL)
 			   "supported by the fbdev driver\n");
 		return FALSE;
 	default:
-		xf86DrvMsg(pScrn->scrnIndex, X_ERROR,
-		           "internal error: unrecognised fbdev hardware type "
-			   "(%d) encountered in FBDevScreenInit()\n", type);
-		return FALSE;
+		xf86DrvMsg(pScrn->scrnIndex, X_WARNING,
+		           "unrecognised fbdev hardware type (%d), treating as packed pixels\n", type);
+		goto packed_cmap;
 	}
 	flags = CMAP_PALETTED_TRUECOLOR;
 	if(!xf86HandleColormaps(pScreen, 256, 8, fbdevLoadPalette, NULL, flags))
