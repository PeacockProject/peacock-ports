[package]
name = "samsung-jflte-fb-logger"
version = "1.0.0"
description = "Samsung jflte framebuffer refresher service for OpenRC"

# Phase 1 schema migration — defaults to layout = "system"; revisit per-port later.
[install]
layout = "system"

[build]
type = "raw"
# No source: stages the peacock-fb-refresher wrapper + OpenRC service into
# $pkgdir. prepare() is a no-op; package() writes the files.
