[package]
name = "firmware-samsung-jflte"
version = "1.0.0"
description = "Placeholder firmware package for Samsung Galaxy S4 (jflte)"

# Phase 1 schema migration — defaults to layout = "system"; revisit per-port later.
[install]
layout = "system"

[build]
type = "raw"
# Placeholder firmware port: no source, no compile. prepare() is a no-op (no
# tarball); package() just produces the (empty) stage dir under $pkgdir.
